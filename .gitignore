.env
pspdev
scrap
.tflite
**target/
.cache
.DS_Store
*.bin
.vscode
models/
benchmarks.json
# Kernel-check results, written by `test-kernels` on both host and device.
test-results.json
meminfo.txt
birdnet.txt
results.txt
out.log

# yes. im sorry.
CLAUDE.md
docs/

examples/birdnet/results.txt
examples/birdnet/golden.json

# Written to the host0: mount by device/build.rs when TOPK prunes the
# classifier; lets birdnet-host project golden.json onto the kept classes.
examples/birdnet/kept_indices.txt

# forward_debug tensor taps (~97 MB per run; regenerate with BIRDNET_TAP=1)
examples/birdnet/device/tap/

# roofline benchmark output
examples/roofline/**/roofline.json

# kernel-plugin build artifacts
kernel-plugin/*.o
kernel-plugin/*.elf
kernel-plugin/*.prx
kernel-plugin/*.S
kernel-plugin/*.a
__pycache__/

# pspbird classifier blobs, published to the host0: mount by pspbird-host.
examples/birdnet/blobs/

# Species photos fetched by examples/birdnet/fetch_images.py (only the script is committed).
examples/birdnet/images/
