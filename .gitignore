.env
pspdev
scrap
.tflite
**target/
.cache
.DS_Store
*.bin
.vscode
models/birdnet/
benchmarks.json
# Kernel-check results, written by `test-kernels` on both host and device.
test-results.json
meminfo.txt
birdnet.txt
results.txt
out.log

# yes. im sorry.
CLAUDE.md
docs/

apps/pspbird/results.txt
apps/pspbird/golden.json

# Written to the host0: mount by device/build.rs when TOPK prunes the
# classifier; lets birdnet-host project golden.json onto the kept classes.
apps/pspbird/kept_indices.txt

# forward_debug tensor taps (~97 MB per run; regenerate with BIRDNET_TAP=1)
apps/pspbird/device/tap/

# roofline benchmark output
examples/roofline/**/roofline.json

# kernel-plugin build artifacts
plugins/kernel-plugin*/*.o
plugins/kernel-plugin*/*.elf
plugins/kernel-plugin*/*.prx
plugins/kernel-plugin*/*.S
plugins/kernel-plugin*/*.a
__pycache__/

# pspbird classifier blobs, published to the host0: mount by pspbird-host.
apps/pspbird/blobs/

# Species photos fetched by apps/pspbird/fetch_images.py (only the script is committed).
apps/pspbird/images/
